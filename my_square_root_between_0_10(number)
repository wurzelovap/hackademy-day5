import math
def my_square_root_between_0_10(number):
  if number >= 0 and number <=10:
    print (math.sqrt(number))
    return(math.sqrt(number))
  else:
    return("number is not in the defined range")
